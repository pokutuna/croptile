// UI constants

// Gutter size for cut canvas (smaller for more image space)
export const CUT_GUTTER_SIZE = 24;

// Gutter size for layout canvas (larger for better ruler visibility)
export const LAYOUT_GUTTER_SIZE = 20;
